version: 0.2

phases:
  install:
    commands:
      - echo "cd into $CODEBUILD_SRC_DIR"
      - cd $CODEBUILD_SRC_DIR
      - wget -q https://ecs-cli-v2-release.s3.amazonaws.com/copilot-linux-v1.34.1
      - mv ./copilot-linux-v1.34.1 ./copilot-linux
      - chmod +x ./copilot-linux

  build:
    commands:
      - echo "Run your tests"
      # - make test

  post_build:
    commands:
      - ls -l
      - export COLOR="false"
      - export CI="true"
      - pipeline=$(cat $CODEBUILD_SRC_DIR/copilot/pipelines/options-ai-platform-main/manifest.yml | ruby -ryaml -rjson -e 'puts JSON.pretty_generate(YAML.load(ARGF))')
      - stages=$(echo $pipeline | jq -r '.stages[].name')

      - >
        for env in $stages; do
          echo "Packaging environment: $env";
          ./copilot-linux env package -n $env --output-dir './infrastructure' --upload-assets;

          if [ $? -ne 0 ]; then
            echo "Environment packaging failed" 1>&2;
            exit 1;
          fi

          echo "Packaging service: api for environment: $env";
          ./copilot-linux svc package -n api -e $env --output-dir './infrastructure' --upload-assets;

          if [ $? -ne 0 ]; then
            echo "Service packaging failed" 1>&2;
            exit 1;
          fi
        done;

      - ls -lah ./infrastructure

artifacts:
  files:
    - "infrastructure/*"
